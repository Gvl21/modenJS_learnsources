/*
JavaScript에서는 모든 값이
truthy 참 같은
falsy 거짓 같은

값을 갖는다.

falst : false, 0, NaN, null, undefined
truthy : falsy 를 제외한 모든 값

모든 문장에 논리 연산을 사용할 수 있다.
&&(AND) ||(OR)  !(NOT)
*/

Boolean(''); // false
Boolean(234); // true
